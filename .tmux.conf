# 1. Set the status bar background to transparent (default)
set-option -g status-style bg=default

# 2. (Optional) Set the foreground (text) color to make it readable
# You can change 'white' to a specific hex code or colour name
set-option -g status-style fg=white

# 3. (Optional) Improve the look of active/inactive window tabs
# This keeps the background transparent but highlights the active window text
set-window-option -g window-status-style bg=default,fg=colour244
set-window-option -g window-status-current-style bg=default,fg=white,bold

# 4. Remove the borders between window names for a cleaner look
set-window-option -g window-status-separator "  "
